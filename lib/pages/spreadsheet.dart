import 'package:flutter/material.dart';

class SpreadsheetPage extends StatelessWidget {
  const SpreadsheetPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Hello, I'm the spreadsheet page"),
    );
  }
}
